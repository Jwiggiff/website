<div class="mac">
  <div class="contain">
    <div class="screen">
      <div class="inner"></div>
    </div>
    <div class="slot">
      <div class="drive"></div>
    </div>
    <div class="logo">
      <img
        src="https://upload.wikimedia.org/wikipedia/commons/thumb/8/84/Apple_Computer_Logo_rainbow.svg/1757px-Apple_Computer_Logo_rainbow.svg.png"
        alt="Apple Logo"
      />
      Macintosh Classic II
    </div>
  </div>
</div>

<style>
  @import url("https://fonts.googleapis.com/css2?family=EB+Garamond&display=swap");

  :root {
    --scale: 1rem;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    height: 100vh;
    display: grid;
    place-items: center;
    background: #222;
    font-family: "EB Garamond", serif;
  }

  .mac {
    aspect-ratio: 3/4;
    width: calc(20 * var(--scale));
    border-radius: calc(1 * var(--scale));
    background-color: #e4d7bf;
    background-image: radial-gradient(
      circle at 100% 0%,
      rgba(255, 255, 255, 0.5),
      transparent
    );
    overflow: hidden;
    box-shadow: 0 0 calc(2 * var(--scale)) rgba(0, 0, 0, 0.5);
  }

  .contain {
    border-top-left-radius: inherit;
    border-top-right-radius: inherit;
    height: calc(100% - 4 * var(--scale));
    box-shadow: 0 0 calc(2 * var(--scale)) calc(1 * var(--scale))
      rgba(0, 0, 0, 0.3);

    display: grid;
    place-items: center start;
    position: relative;
  }

  .screen {
    place-self: start center;
    margin-top: calc(2 * var(--scale));
    aspect-ratio: 5/4;
    width: 80%;
    border-radius: calc(0.5 * var(--scale));
    background-image: conic-gradient(
      rgba(0, 0, 0, 0.6) 51deg,
      rgba(0, 0, 0, 0.3) 53deg,
      rgba(0, 0, 0, 0.3) 127deg,
      rgba(255, 255, 255, 0.5) 129deg,
      rgba(255, 255, 255, 0.5) 231deg,
      rgba(0, 0, 0, 0.3) 233deg,
      rgba(0, 0, 0, 0.3) 307deg,
      rgba(0, 0, 0, 0.6) 309deg
    );

    position: relative;
    display: grid;
    place-items: center;

    .inner {
      content: "";
      position: absolute;
      width: calc(100% - var(--scale));
      height: calc(100% - var(--scale));
      border-radius: inherit;
      background: #4b5c54;
      box-shadow: 0 0 calc(2 * var(--scale)) rgba(0, 0, 0, 0.5) inset;
    }
  }

  .slot {
    width: 100%;
    height: calc(0.6 * var(--scale));
    background-image: linear-gradient(
      rgba(0, 0, 0, 0.25),
      transparent 30%,
      transparent 70%,
      rgba(0, 0, 0, 0.1)
    );
    position: relative;

    .drive {
      position: absolute;
      width: 45%;
      height: 100%;
      right: calc(2 * var(--scale));

      background: black;
    }
  }

  .logo {
    display: flex;
    align-items: center;
    gap: var(--scale);
    padding: 0 calc(1.5 * var(--scale));
    margin-bottom: calc(1 * var(--scale));

    img {
      width: calc(1.25 * var(--scale));
    }
  }
</style>
